# 🚀 Деплой Fudly Bot на Railway

⚠️ **ВАЖНО:** Обязательно настройте Volume (Шаг 2), иначе все данные будут теряться при каждом редеплое!

## 📋 Предварительная подготовка

### ✅ Что уже готово:
- ✅ Проект очищен от временных файлов
- ✅ Локализация RU/UZ полностью синхронизирована (139 ключей)
- ✅ Фильтры по городам работают корректно
- ✅ Автоудаление истекших товаров настроено
- ✅ Все зависимости в requirements.txt

## 🔧 Шаг 1: Подготовка Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Создайте новый проект: **New Project** → **Deploy from GitHub repo**
4. Выберите репозиторий **shaxa2505/fudly-bot**

## � Шаг 2: Настройка постоянного хранилища (ВАЖНО!)

**⚠️ БЕЗ ЭТОГО ШАГА ВСЕ ДАННЫЕ БУДУТ ТЕРЯТЬСЯ ПРИ КАЖДОМ РЕДЕПЛОЕ!**

Railway использует эфемерное хранилище - при каждом деплое контейнер пересоздаётся и **все файлы удаляются**. Нужно создать Volume для сохранения базы данных.

### Создание Volume:

1. В Railway откройте ваш проект
2. Перейдите в **Settings** → **Volumes**
3. Нажмите **+ New Volume**
4. Укажите:
   - **Mount Path:** `/data`
   - **Size:** 1 GB (достаточно для старта)
5. Нажмите **Add**

### Настройка переменной окружения:

Теперь нужно указать боту, что база данных находится в `/data`:

1. Перейдите в **Variables**
2. Добавьте переменную:
   ```
   DATABASE_PATH=/data/fudly.db
   ```

Теперь база данных будет храниться в Volume и **НЕ будет удаляться** при редеплое! ✅

## �🔐 Шаг 3: Настройка переменных окружения

В настройках проекта Railway добавьте переменные:

```bash
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_ID=ваш_telegram_id
USE_WEBHOOK=true
WEBHOOK_URL=https://ваш-проект.up.railway.app

# ВАЖНО: Путь к базе данных в Volume
DATABASE_PATH=/data/fudly.db

# ОПЦИОНАЛЬНЫЕ
PORT=8000
```

**Опциональные переменные:**
```bash
TELEGRAM_SECRET_TOKEN=random_secret_string_123  # Дополнительная безопасность webhook
LOG_LEVEL=INFO
```

### Как получить значения:

**TELEGRAM_BOT_TOKEN:**
1. Откройте @BotFather в Telegram
2. Создайте нового бота командой `/newbot` или используйте существующего
3. Скопируйте токен (формат: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

**ADMIN_ID:**
1. Напишите @userinfobot в Telegram
2. Скопируйте ваш ID (формат: `253445521`)

**WEBHOOK_URL:**
1. После первого деплоя Railway даст вам URL типа `fudly-bot-production.up.railway.app`
2. Используйте `https://ваш-домен.up.railway.app` (БЕЗ /webhook)

## 📦 Шаг 3: Первый деплой

Railway автоматически:
1. Обнаружит `runtime.txt` → установит Python 3.11.9
2. Прочитает `requirements.txt` → установит зависимости
3. Запустит `Procfile` → выполнит `web: python bot.py`

**Ожидаемое время:** 2-3 минуты

## ✅ Шаг 4: Проверка

### Проверьте логи:
```
Railway Dashboard → ваш проект → Deployments → View Logs
```

### Ожидаемый вывод:
```
✅ Админ установлен: 253445521
🚀 Запуск бота Fudly (Production Optimized)...
📊 База данных: /data/fudly.db  ← ВАЖНО: должен быть /data/fudly.db
👑 Главный админ: 253445521
🌍 Языки: Русский, Узбекский
✅ Webhook установлен: https://...
✅ Бот успешно запущен!
```

**⚠️ Проверьте путь к БД:** Если видите `📊 База данных: fudly.db` (без `/data/`), значит переменная `DATABASE_PATH` не установлена!

### Протестируйте бота:
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте регистрацию
4. Проверьте меню покупателя/партнёра
5. Админ-панель (если вы указали свой ID в ADMIN_ID)

## 🔄 Шаг 5: Обновления

### Автоматический деплой при push:
```bash
git add .
git commit -m "update: описание изменений"
git push origin main
```

Railway автоматически задеплоит новую версию!

## 🐛 Возможные проблемы

### ❌ Данные теряются после каждого редеплоя:
**Причина:** База данных не настроена на Volume

**Решение:**
1. Создайте Volume в Railway (Settings → Volumes → New Volume)
2. Mount Path: `/data`
3. Добавьте переменную `DATABASE_PATH=/data/fudly.db`
4. Редеплойте бота
5. Проверьте логи: должно быть `📊 База данных: /data/fudly.db`

### ❌ 500 Internal Server Error в webhook:
**Решение:**
- Проверьте что переменная `TELEGRAM_BOT_TOKEN` установлена (не `BOT_TOKEN`)
- Убедитесь что `SECRET_TOKEN` не установлен или установлен правильно
- Проверьте логи: `Railway → Deployments → View Logs`
- Ошибка `name 'SECRET_TOKEN' is not defined` исправлена в последней версии

### ❌ Бот не отвечает:
- Проверьте WEBHOOK_URL (должен быть https без /webhook в конце)
- Проверьте TELEGRAM_BOT_TOKEN (правильный токен от @BotFather)
- Проверьте логи в Railway
- Убедитесь что webhook установлен: проверьте в логах строку `✅ Webhook установлен:`

### База данных теряется:
- **Railway использует эфемерное хранилище!**
- **РЕШЕНИЕ:** Настройте Volume (см. Шаг 2 выше)
- Без Volume все данные удаляются при каждом деплое
- С Volume данные сохраняются навсегда

### Нужна миграция на PostgreSQL?
Для больших проектов (1000+ пользователей) рекомендуется PostgreSQL:

1. **Вариант 1: Railway Postgres**
   - Railway Dashboard → New → Database → PostgreSQL
   - Автоматически создаёт `DATABASE_URL`
   - Нужно будет изменить код для работы с PostgreSQL

2. **Вариант 2: Supabase** (бесплатный PostgreSQL)
   - Зарегистрируйтесь на https://supabase.com
   - Создайте проект
   - Скопируйте Connection String
   - Добавьте в Railway: `DATABASE_URL=postgresql://...`

3. **Вариант 3: SQLite + Volume** (текущий вариант)
   - ✅ Работает до 1000 активных пользователей
   - ✅ Не требует изменения кода
   - ✅ Бесплатно
   - ⚠️ Не масштабируется для очень больших нагрузок

**Для старта и MVP используйте SQLite + Volume!**

### Timeout ошибки:
- Увеличьте таймауты в bot.py
- Проверьте медленные SQL запросы

## 📊 Мониторинг

Railway показывает:
- CPU usage
- Memory usage
- Network
- Request logs

## 💰 Стоимость

- **Free tier:** $5 в месяц бесплатно
- **Достаточно для:** ~500-1000 активных пользователей
- **Upgrade при необходимости:** от $5/месяц

## 🎉 Готово!

Бот развёрнут и работает 24/7 на Railway!

---

## 📞 Поддержка

Проблемы? Проверьте:
1. Логи в Railway Dashboard
2. Переменные окружения
3. GitHub репозиторий синхронизирован

Успешного запуска! 🚀
