# Fudly Telegram Bot - Dependencies for Railway

# Telegram Bot Framework (REQUIRED)
aiogram>=3.0.0

# Environment variables (REQUIRED)
python-dotenv>=0.19.0

# Web server for webhook mode (REQUIRED for Railway)
aiohttp>=3.9.0

# HTTP client
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Production Features (optional - graceful fallback if not installed)
# Uncomment if needed:
# redis>=4.5.0                   # For Redis caching
# python-json-logger>=2.0.0      # For structured logging
# Pillow>=10.0.0                 # For image processing
# sentry-sdk>=1.40.0             # For error tracking
